<nav id="mainNav" class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to root_path do %>
        <%= image_tag "framgia2.png", class: "header-img" %>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <%= link_to t("categories.category"), categories_path %>
          </li>
          <li>
            <%= link_to t("word"), words_path %>
          </li>
          <li>
            <%= link_to t("users.index"), users_path %>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= t :account %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t(:profile), current_user %></li>
              <li><%= link_to t(:setting),
                edit_user_registration_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to link_to t("users.logout"),
                  destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li>
            <%= link_to t("users.login"), new_user_session_path %>
          </li>
          <li>
            <%= link_to t(:sign_up), new_user_registration_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
